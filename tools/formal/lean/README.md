# Historical Lean Workspace Path

The live Lean workspace moved to `/hyperopen/spec/lean/` on 2026-03-28.

This directory now exists only so historical ExecPlans that deep-link into the old `tools/formal/lean/**` tree still resolve to something useful. Do not point current tooling at this path.
