(ns hyperopen.websocket.instrument-coverage-test
  (:require [cljs.test :refer-macros [deftest is]]
            [hyperopen.domain.market.instrument-test]))

(deftest instrument-coverage-bridge-loads-domain-market-suite-test
  (is true))
