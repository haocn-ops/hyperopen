# Open DEXHelm Mainnet only after governed readiness

This ExecPlan is a living document. It follows `/Users/zh/Documents/Hyperopen/docs/PLANS.md` and `/Users/zh/Documents/Hyperopen/.agents/PLANS.md`; keep `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` current as evidence arrives. This plan was created from the direct maintainer request in the current session. It is the durable launch record, not a replacement for a GitHub issue or PR.

## Purpose / Big Picture

DEXHelm currently gives users a deliberately Testnet-only terminal. The Mainnet hostname, `https://app.dexhelm.com`, must continue to return the intentional HTTP 503 until the complete Testnet fund-flow is accepted, the production owner signs the operations and compliance checklist, and a separate current-session authorization approves the Phase 9 Mainnet release. After those gates, a maintainer can publish one deterministic release in which Testnet still forces Testnet, the Mainnet host forces Mainnet, and the public host matrix, health endpoint, headers, and proxy boundaries are verifiably correct. No real-fund action is implied by technical opening.

## Context References

Public refs:

- Direct maintainer request on 2026-08-05: safely open DEXHelm Mainnet only after full Testnet fund-flow acceptance, production operations/compliance readiness, deterministic Mainnet technical boundaries, full gates, Phase 8 owner signoff, and separate current-session Phase 9 deploy authorization.
- Direct maintainer request on 2026-08-05: treat the reproduced Positions `Close All` no-op as a Mainnet-blocking defect. The smallest safe repair must require an explicit destructive confirmation, submit only reduce-only closes for every current nonzero position, expose in-flight/error/success state, and remain unavailable in read-only, spectate, and empty states.

Repo artifacts:

- `/Users/zh/Documents/Hyperopen/AGENTS.md`
- `/Users/zh/Documents/Hyperopen/docs/PLANS.md`
- `/Users/zh/Documents/Hyperopen/docs/MULTI_AGENT.md`
- `/Users/zh/Documents/Hyperopen/docs/WORK_TRACKING.md`
- `/Users/zh/Documents/Hyperopen/.agents/skills/deploy-hyperopen-cloudflare/SKILL.md`
- `/Users/zh/Documents/Hyperopen/.agents/skills/deploy-hyperopen-cloudflare/references/zero-to-live.md`
- `/Users/zh/Documents/Hyperopen/docs/runbooks/hyperliquid-testnet-implementation.md`
- `/Users/zh/Documents/Hyperopen/docs/exec-plans/completed/2026-07-25-dexhelm-public-brand-release.md`
- `/Users/zh/Documents/Hyperopen/docs/exec-plans/active/2026-07-22-worker-browser-qa-remediation.md`
- `/Users/zh/Documents/Hyperopen/wrangler.jsonc`
- `/Users/zh/Documents/Hyperopen/wrangler.mainnet-opening.jsonc`
- `/Users/zh/Documents/Hyperopen/workers/hyperopen-worker.mjs`
- `/Users/zh/Documents/Hyperopen/config/white-label/dexhelm.json`
- `/Users/zh/Documents/Hyperopen/tools/cloudflare/build_dexhelm_mainnet_opening_candidate.mjs`
- `/Users/zh/Documents/Hyperopen/tools/release-assets/security_headers.mjs`
- `/Users/zh/Documents/Hyperopen/out/cloudflare/dexhelm-mainnet-opening/_headers`

Local scratch refs (non-authoritative): none.

## Progress

- [x] (2026-08-05) Read the governed planning, multi-agent, work-tracking, deployment, zero-to-live, and Hyperliquid Testnet runbook instructions.
- [x] (2026-08-05) Recorded source HEAD `3289a95a969373e863f1955f8de919c27914372e`, branch `main`, and a clean worktree; `fork/main` points at the same commit.
- [x] (2026-08-05) Inspected the checked-in DEXHelm release boundary: four custom domains, `workers_dev: false`, `out/white-label/dexhelm`, and Testnet-only HyperUnit binding.
- [x] (2026-08-05) Ran artifact preflight: 33 passes, 0 failures, and only the documented JDK 21 and Node 25 localStorage warnings.
- [x] (2026-08-05) Captured the historical bundle-budget warning from the prior branded release: main bundle 653,917 gzip bytes versus the 640,000-byte advisory target (+13,917).
- [x] (2026-08-05) Created and schema-validated `tmp/multi-agent/dexhelm-mainnet-opening/acceptance-tests.proposal.json` and the merged `approved-test-contract.json`; the frozen contract contains six happy-path and five adversarial cases across release, Worker, deployment handoff, and Playwright surfaces.
- [x] (2026-08-05) Ran `git diff --check -- docs/exec-plans/active/2026-08-05-dexhelm-mainnet-opening.md` and `npm run lint:docs`; both passed. The plan file is intentionally the only tracked-scope worktree addition at this milestone.
- [x] (2026-08-05) Implemented the isolated Mainnet opening candidate: explicit Mainnet tenant config, paired Testnet/Mainnet artifact builder, fixed upstream bindings, explicit `HYPEROPEN_MAINNET_ENABLED` gate, host/path isolation, and candidate Wrangler contract.
- [x] (2026-08-05) Mainnet candidate deterministic tests pass: Worker/rewrite/security coverage `40/40`, release-asset coverage `51/51`, and Mainnet/tenant acceptance coverage `9/9`.
- [x] (2026-08-05) Built the Testnet release and Mainnet opening candidate repeatedly with JDK 21: 0 compile warnings, 0 authored XSS sinks, Testnet artifact preflight `33 passed / 0 failed`, Mainnet candidate `106` artifacts and `48` release scripts.
- [x] (2026-08-05) Candidate and Testnet Wrangler dry-runs exited `0`; candidate read `116` assets with explicit Mainnet enablement and fixed Mainnet/Testnet upstreams, while production config remained Testnet-only.
- [x] (2026-08-05) Release SEO Playwright passed `7/7`; DEXHelm white-label Playwright passed `4/4` at `375/768/1280/1440` after using the tenant's actual `dark` theme and enabled-route contract.
- [x] (2026-08-05) Browser MCP inspection covered Mainnet and Testnet at `375/768/1280/1440`, canonical network redirects, health/proxy boundaries, focus, native controls, overflow, and performance; sessions and Wrangler processes were stopped. Overall browser-QA verdict is `FAIL` because the Mainnet page has no visible `Mainnet` environment label and the header brand name uses responsive negative `letter-spacing`.
- [x] (2026-08-05) Final candidate local Wrangler HTTPS checks passed with separate exact local upstreams: app canonicalized to `mainnet`, Testnet canonicalized to `testnet`, both health checks returned `200`, each same-network proxy returned non-5xx JSON, and both cross-network proxies returned `404`; all local Wrangler sessions were stopped.
- [x] (2026-08-05) Rebuilt the candidate after the UX fixes and passed the dedicated Mainnet opening Playwright contract `8/8` at `375/768/1280/1440`: both hosts visibly identify their network, header brand zero tracking is CSSOM-normalized, canonical URLs are correct, no wallet/signature/funding/order request occurs, and no non-local-noise console error remains.
- [x] (2026-08-06) Re-ran the complete corrected-environment matrix after the Close All and Trusted Types repairs: `35/35` gates passed, including `5882` ClojureScript tests / `32817` assertions and `561` websocket tests / `3184` assertions. The aggregate gate record is `6637` tests / `36213` assertions.
- [x] (2026-08-06) Added the fail-closed Hyperliquid `legalCheck` gate to deposit and withdrawal open/submit flows. Blocked or unavailable checks stop before any signer call; pure decision tests and signer-zero-call regressions pass. Promise normalization now accepts native JS thenables, and lifecycle tests inject an explicit allowed decision.
- [x] (2026-08-06) Re-ran the corrected validation after the legal-check fix: `npm test` passed `5888` tests / `32843` assertions, `npm run test:websocket` passed `561` / `3184`, `npm run check` passed, and the aggregate matrix passed `35/35` with `6643` tests / `36239` assertions.
- [x] (2026-08-05) Mainnet candidate dry-run passed after the UX fixes: `npm run cloudflare:check:mainnet-candidate` read `116` assets, bound the explicit Mainnet enablement flag and both fixed HyperUnit origins, and exited at `--dry-run` without upload.
- [x] (2026-08-06) Rebuilt the final Mainnet candidate after the legal-check and cache-policy repairs: `106` artifacts, `48` release scripts, `0` authored XSS sinks; Worker tests `49/49`, release-asset tests `52/52`, artifact preflight `33 passed / 0 failed`, and Wrangler dry-run read `116` assets with no upload. Current candidate `tenant-manifest.json` SHA-256 is `0d8b9b3e9d8c97658a4bce775d6c5fef39d53a03a8e588f27901d22022e09ca2`.
- [x] (2026-08-06) Re-ran the governed Mainnet-opening Playwright contract on the final candidate: all `8/8` Mainnet/Testnet cases passed at `375/768/1280/1440`, with forced network labels, exact immutable caching on the document-discovered fingerprinted CSS and JavaScript for both hosts, and no wallet, signature, funding, or order action. The candidate manifest remained `0d8b9b3e9d8c97658a4bce775d6c5fef39d53a03a8e588f27901d22022e09ca2` after the run.
- [x] (2026-08-06) Repaired the Mainnet fingerprint cache mismatch within the release boundary only. The candidate builder now derives immutable paths through the shared release policy, adds `22` exact `/__hyperopen_mainnet/css|js/` rules to the authoritative root `_headers`, refreshes the Testnet and outer candidate digests in the correct order, and leaves worker scripts, metadata, service worker, documents, health, and proxy responses non-immutable. The focused RED test and four Mainnet browser failures reproduced `max-age=0`; after the fix the Cloudflare suite passed `49/49`, Playwright passed `8/8`, the full matrix passed `35/35`, and no Worker, `src/**`, wallet, fund, or production state changed.
- [x] Resolve the Browser MCP Mainnet UX blockers: show an unambiguous visible Mainnet environment indicator and make header brand `letter-spacing: 0`; rerun the governed four-viewport Browser QA and require every pass to be `PASS`.
- [x] (2026-08-05) User confirmed the already-connected Testnet wallet as the disposable test wallet and approved a total disposable ceiling of `10 USDC` equivalent; all wallet confirmations remain manual. The deposit, enable, order/close, transfer, and withdrawal evidence is still pending.
- [x] (2026-08-05) Resolved the Testnet wallet safety mismatch by recording the user's explicit expanded authorization for the current account and existing position; no agent wallet action was taken. The account is now visibly flat (`0.0000 BTC`, no active positions, no open orders), but its approximately `977.60 USDC` balance remains outside the original `10 USDC` disposable scope and must not be moved without a separate action/amount authorization.
- [x] (2026-08-05) After the user expanded the account scope and confirmed the close action, the live Testnet page showed `0.0000 BTC`, `No active positions`, and `No open orders`; no agent-submitted order or wallet confirmation was performed.
- [x] (2026-08-06) Read-only Chrome verification established the separately authorized `11 USDC` Testnet order round trip: Trade History shows a BTC `Open Long` at 20:54:22 for `0.00017 BTC` / `10.95 USDC` and a BTC `Close Long` at 21:03:46 for the same `0.00017 BTC` / `10.93 USDC`, each with a Hyperliquid explorer transaction link. The live account remains flat at `0.0000 BTC`, `No active positions`, `US$0.00` unrealized PNL, and no visible open-order evidence.
- [x] (2026-08-06) Independently evidenced the authorized Perps -> Spot -> Perps `1 USDC` round trip through `userNonFundingLedgerUpdates`: the first `accountClassTransfer` moved `1.0 USDC` out of Perps at `2026-08-05 20:43:15 CST` (`toPerp: false`), and the second returned `1.0 USDC` at `20:45:08 CST` (`toPerp: true`). Arbitrum Sepolia correctly returned no transaction receipts for those two Hyperliquid-internal ledger hashes; final Spot state is `0.0 USDC` and Perps remains the funded account.
- [ ] (2026-08-06) `BLOCKED`: the user separately authorized a minimum `5 USDC` withdrawal to the currently connected Testnet wallet and manually handled each MetaMask signature. Two controlled attempts returned the same upstream response, `Error withdrawing from bridge`. Read-only verification after each attempt found no new `userNonFundingLedgerUpdates`, no Perps debit, no payout transaction, and an unchanged `977.563573 USDC` withdrawable balance. Hyperliquid Testnet `/info`, inert `/exchange`, and WebSocket smoke checks passed; the Arbitrum Sepolia Bridge2 contract held approximately `988,880,295.641721 USDC2`; and the repository's `withdraw3` action and EIP-712 fields match the current official Hyperliquid Python SDK. Stop retries and keep Phase 7 incomplete until a later minimum withdrawal is accepted and both the Perps debit and USDC2 delivery are independently verified.
- [x] (2026-08-06) Historical root-cause lead, now superseded: Hyperliquid `legalCheck` returned `acceptedTerms: true`, `userAllowed: true`, and `restrictions: "a"` (`BlockActions`) for both the current Testnet account and the zero address from the current operator network. The current official Hyperliquid frontend maps `"a"` to `BlockActions`, a source-based restriction that blocks deposits/withdrawals while leaving the account itself allowed. At the time of the earlier failed withdrawals, DEXHelm had no `legalCheck`/`BlockActions` integration and exposed a withdrawal signature that the upstream later rejected as a generic Bridge error; the client now fails closed before any signer call when the check is blocked or unavailable. This remains valid protection but no longer explains the later failure after the restriction changed to `"o"`.
- [x] (2026-08-06) Rechecked Hyperliquid's public `legalCheck` read-only endpoint for the zero address on both Testnet and Mainnet. Both still return `acceptedTerms: true`, `userAllowed: true`, `restrictions: "a"`; no withdrawal retry, signer call, or jurisdiction workaround was made.
- [x] (2026-08-06) Confirmed the local candidate contains the client-side fix for the withdrawal issue: the `legalCheck` `BlockActions` result produces a jurisdiction-blocked message and zero signer calls before `withdraw3`; the remaining inability to settle is upstream Hyperliquid eligibility, not a client-side signature or request-shape defect. This candidate fix is not deployed while Phase 8 remains blocked.
- [x] (2026-08-06 18:23 CST) Rechecked `legalCheck` after it changed: the connected account and zero address now return `acceptedTerms: true`, `userAllowed: true`, and `restrictions: "o"` on both Testnet and Mainnet. A separately user-confirmed `10 USDC` Testnet withdrawal still returned `Error withdrawing from bridge`, so the earlier `BlockActions` classification is not the current cause of the bridge rejection.
- [ ] (2026-08-06 18:26 CST) `BLOCKED`: post-attempt read-only checks show no `userNonFundingLedgerUpdates` since `2026-08-06 00:00 CST`, unchanged `977.563573 USDC` Perps withdrawable balance, no positions, and no Arbitrum Sepolia USDC2 transfer for the wallet from `2026-08-05 07:28 CST` through `2026-08-06 18:26 CST`. The wallet's existing `15 USDC2` therefore predates this attempt. Record this as a third upstream bridge rejection with no debit or payout, and do not retry until Hyperliquid confirms service recovery or a concrete client request difference is identified.
- [x] (2026-08-06) Refreshed the active plan for the DEXHelm Bridge2 USDC withdrawal parity repair. The official exchange documentation updated 2026-07-31 states a `$1` `withdraw3` fee and finalization in about five minutes; the official Bridge2 page states `3-4 minutes`; the official docs' `5 USDC` minimum applies to deposits, not withdrawals. The plan now scopes a user-facing `3-5 minutes` estimate, `1 USDC` withdrawal fee, no undocumented USDC withdrawal minimum, and unchanged `withdraw3` request/signing fields.
- [x] (2026-08-06 19:57 CST) Committed and pushed the Bridge2 parity implementation as `ff8d02b1` to `fork/main`; `npm test` passed `5892/32856`, `npm run test:websocket` passed `561/3184`, `npm run check` passed with the documented local-listen permission, release SEO passed `7/7`, artifact preflight passed `33/0`, Cloudflare dry-run exited `0`, and the focused Bridge2 withdrawal Playwright passed `1/1`. The existing Testnet-only Worker was deployed as version `20` (`4bdd9636-2e89-4f4f-8e49-7a4a198959eb`) with public monitor results apex `200`, Testnet `200`, Mainnet `503`, status `200`, and Testnet health `200`; no wallet, signature, fund, DNS, or Mainnet-opening action occurred.
- [ ] Implement the Bridge2 withdrawal parity contract in the funding domain, modal view model, deterministic tests, and focused Playwright flow; keep the Testnet settlement blocker and Mainnet authorization gates unchanged.
- [x] (2026-08-06) Maintainer confirmed that no legal reviewer, monitor alert recipient, incident owner/channel, independent outage path, or rollback owner is currently available. These remain explicit Phase 8 blockers; no names or approvals were invented.
- [ ] Assign named production, monitoring, alerting, incident, support, rollback, risk/privacy/terms, AGPL/source, and affiliate owners and attach their evidence. A named monitor alert recipient, incident owner/on-call route, independent outage-communication path, and rollback owner remain absent; privacy/Terms approval is also blocked because the required legal practice profile is unavailable.
- [x] Confirm the exact current Worker account, custom-domain certificate/DNS state, and a verified rollback version via Wrangler; the supplied current version is `f26e000b-532c-44cd-8de3-cd04aab40fb2`.
- [x] (2026-08-06) Completed the read-only rollback drill without mutation: `wrangler versions list` reconfirmed current version 18 (`f26e000b-532c-44cd-8de3-cd04aab40fb2`) and immediately preceding version 17 (`32112083-f3ac-4077-8138-5170efe4a7ca`); `wrangler versions view` resolved version 17 with a fetch handler, Static Assets, Testnet-only HyperUnit binding, and the expected header policy. The rehearsed post-rollback public probe passed against the unchanged live version with `200/200/503/200/200`. Any real rollback still requires separate authorization for the exact version.
- [x] Re-run the public four-host/status matrix from a network-enabled operator environment; the four hosts resolve over HTTPS and return the expected pre-Phase-9 statuses.
- [x] (2026-08-06) Ran the full release/browser/local-Worker/dry-run/gate sequence on the final release candidate and obtained the user's conditional Phase 8 production-owner signoff. The signoff does not waive the separately listed Testnet settlement, legal approval, monitoring/incident ownership, outage-communication, or rollback-owner gates.
- [x] (2026-08-05) User explicitly signed as production owner for Phase 8; this attestation does not waive the still-missing privacy/terms, named monitor-alert recipient, incident-owner/channel, independent outage-communication, or Testnet-settlement evidence.
- [x] (2026-08-06) Rebuilt the current generic, DEXHelm branded, and Mainnet candidate release artifacts and measured the main module at `635,303`, `635,653`, and `635,653` gzip bytes respectively. All are below the unchanged `640,000` advisory target; the historical `+13,917` overage is resolved without a budget ratchet or unmeasured optimization code.
- [x] (2026-08-05) Created the separate Phase 9 implementation record at `docs/exec-plans/active/2026-08-05-dexhelm-mainnet-phase9.md`; it preserves the authorization boundary and does not authorize deployment.
- [x] (2026-08-05) Received explicit current-session authorization from the maintainer to perform Phase 8/9 operations, subject to the remaining readiness gates; this does not authorize wallet confirmations or real-fund actions.
- [x] (2026-08-05) Reproduced the Mainnet-blocking Positions `Close All` defect: the visible desktop header dispatches `:actions/trigger-close-all-positions`, the runtime collaborator resolves it, and `position_overlay_actions/trigger-close-all-positions` returns `[]`, so the click produces neither confirmation nor order nor feedback.
- [x] (2026-08-06) Completed the governed Positions Close All confirmation and all-or-nothing reduce-only batch-close lifecycle described in Milestone 5A. Focused Playwright passed `4/4` at `375/768/1280/1440`; desktop cases prove explicit confirmation and exactly one two-leg reduce-only IOC batch, while mobile cases prove the control remains unavailable.
- [x] (2026-08-05) Materialized the deterministic Milestone 5A RED suite across the ten approved Close All test surfaces and regenerated `test/test_runner_generated.cljs` (`863` namespaces). With the documented JDK 21 environment, `npx shadow-cljs --force-spawn compile test && node out/test.js` fails as intended because `hyperopen.account.history.close-all-positions` is absent; no production source, wallet, exchange, or network path was changed or invoked.
- [x] (2026-08-05) Materialized the worker-owned Close All implementation: pure unfiltered snapshot and all-or-nothing batch planner, mandatory confirmation lifecycle, dedicated batch effect, runtime/schema registration, and desktop-only UI gate. `npx shadow-cljs --force-spawn compile app` passed with JDK 21 and no warnings; no wallet, exchange, or network path was invoked.
- [x] (2026-08-06) Completed deterministic Close All validation: the generated `effect-order-contract` policy corpus was refreshed through `bb tools/formal.clj sync --surface effect-order-contract`, formal verification passed, and the complete ClojureScript suite passed `5882` tests / `32817` assertions with no failures.
- [x] (2026-08-06) Repaired the Close All Playwright fixture to use the current memory-only agent-session contract: `:session` mode plus the process-local lockbox, with the signed exchange boundary still simulated. The focused matrix passed `4/4`; no wallet extension, external exchange, or real order was used.
- [x] (2026-08-06) Replaced the three Close All raw color literals with governed theme tokens. `npm run lint:theme-colors` passes and the complete gate matrix is `35/35`.
- [x] (2026-08-06) Fixed the release Trusted Types policy to allow only the three checked-in fixed worker paths while continuing to reject query variants, wrong directories, and unknown worker scripts. Worker tests pass `48/48` and release-asset tests pass `52/52`.
- [x] (2026-08-06) Completed governed trade-route browser QA at `375/768/1280/1440`: visual evidence, native controls, styling consistency, interaction, layout regression, and jank/performance all passed with zero issues. The focused Close All Playwright suite supplies the destructive confirmation lifecycle coverage that is not present in the default-state design sampling.
- [x] (2026-08-06) Ran the repository-owned public monitor once from a network-enabled environment: apex `200`, Testnet `200`, intentionally closed Mainnet `503`, status `200`, and Testnet health `200`, all matching policy. This proves the probe but does not supply a named alert recipient, incident channel, independent outage owner, or a pushed/scheduled workflow.
- [x] (2026-08-06) Rebuilt the Mainnet candidate after the Trusted Types repair: `106` artifacts, `48` release scripts, and `0` authored XSS sinks. Wrangler read `116` assets with explicit Mainnet enablement and fixed Mainnet/Testnet upstreams, then exited successfully at `--dry-run`; no upload or deployment occurred.
- [x] (2026-08-06) Proxy authority-relative edge tests passed in `tools/cloudflare/mainnet_opening_edge_cases.test.mjs`, covering double-slash and backslash authority escapes without upstream proxy calls.
- [x] (2026-08-06) Re-ran all independent release checks without publishing: Worker tests `45/45`, release-asset tests `51/51`, Mainnet/Testnet Playwright `8/8`, candidate build `106` artifacts with zero authored XSS sinks, artifact preflight `33 passed / 0 failed`, and Wrangler dry-run exit `0` after reading `116` assets. Candidate manifest SHA-256 is `04e2c2597cfeedaace31e4e9b350de1e6e6a5e16f134e6121109bf7a5748d7a0`; Testnet/Mainnet config digests are `995F705A89523B891DF0BA64C80204B44BB97AF6985A0529FA34150C1A07365C` and `336D5A871993CE9122249AACDD6D5A2BC6A57114C1940B0689CEAA83E002BF68`.
- [x] (2026-08-06) Reconfirmed the existing production state without mutation: Worker `hyperopen` remains on version `f26e000b-532c-44cd-8de3-cd04aab40fb2` (version 18), with immediately preceding version `32112083-f3ac-4077-8138-5170efe4a7ca` (version 17) recorded for rollback review. Public apex/Testnet/Mainnet/status status codes remain `200/200/503/200`; Testnet health is `200` with `no-store`, its same-network fee probe is `200`, cross-network and generic proxy probes are `404`, and the public logo is `200 image/svg+xml`.
- [x] (2026-08-06) Published the reviewed Testnet-only Worker update from source commit `22e1c355aeaae6eba3ff07c60591f7d81e735727` after explicit deployment authorization. Wrangler deployed version 19 (`ce68c039-4f88-4640-ad4d-9271a1a3e48a`) to the existing `hyperopen` Worker with only `HYPERUNIT_TESTNET_URL`; the prior verified production version `f26e000b-532c-44cd-8de3-cd04aab40fb2` remains the rollback candidate. Post-deploy verification passed: deployment headers verified, Testnet same-network fee proxy `200`, Mainnet and generic proxy paths `404`, public apex/Testnet/Mainnet/status matrix `200/200/503/200`, Testnet health `200` with `no-store`, and logo `200 image/svg+xml`. No Mainnet candidate config, DNS, wallet, signature, or fund state changed.

## Surprises & Discoveries

- Observation: the current Worker is intentionally Testnet-only even though the generic ClojureScript build defaults shown in `shadow-cljs.edn` use Mainnet for ordinary development/test compilation.
  Evidence: `config/white-label/dexhelm.json` declares `"hyperliquid-network": "testnet"`; `wrangler.jsonc` binds only `HYPERUNIT_TESTNET_URL`; artifact preflight reports that release JavaScript contains only the Testnet same-origin proxy base; `workers/hyperopen-worker.mjs` returns `mainnetClosedResponse()` for `app.dexhelm.com` before assets or proxy handling.
- Observation: the checked-in public host policy is exact and does not publish a Workers development hostname.
  Evidence: `wrangler.jsonc` has `workers_dev: false` and routes only `dexhelm.com`, `app.dexhelm.com`, `testnet.dexhelm.com`, and `status.dexhelm.com`; preflight passes the corresponding contract.
- Observation: the current Worker version supplied by the maintainer (`f26e000b-532c-44cd-8de3-cd04aab40fb2`) is not present in checked-in historical plans, so repository history alone cannot prove its account, route, or rollback status.
  Evidence: checked-in records document earlier versions such as `906ea1c8-e2cd-42d4-915f-db8d0f8ebe56` (DEXHelm brand release), `588a4869-597b-4fd0-b744-42a631b7ec6f` (Testnet runbook), `22f0460a-f31c-4b52-8f44-038d5afffec0`, `2713e467-dd3b-4e29-b320-b338937a8381`, and `9e842a8f-8eb6-412f-a105-c5d15df6da66`; all must be revalidated with `wrangler versions list` before treating one as rollback-safe.
- Observation: the last branded release carried an advisory, not a failing, bundle budget overage.
  Evidence: `docs/exec-plans/completed/2026-07-25-dexhelm-public-brand-release.md` records the historical 653,917 gzip bytes versus the 640,000-byte target, a +13,917 delta. A fresh rebuild on 2026-08-06 measured the current DEXHelm branded and Mainnet candidate modules at 635,653 gzip bytes, so the warning is remediated without a budget ratchet.
- Observation: the current launch is blocked by operational and transaction evidence, not by the read-only artifact preflight.
  Evidence: no current-session Testnet deposit/settlement, order/close, Perps/Spot round trip, and withdrawal-chain delivery record is attached; external monitoring, incident ownership, compliance signoff, and a completed rollback drill are also absent.
- Observation: the initial restricted sandbox could not complete public DNS/HTTP verification, but the authorized network probe later completed it.
  Evidence: the first read-only curl matrix returned curl error 6 (`Could not resolve host`); the subsequent authorized probe resolved all four hosts and recorded the expected pre-Phase-9 status/header matrix below. The initial failure is environmental and is not used as release evidence.
- Observation: the authorized network now resolves and serves the four public hosts from the expected Cloudflare account; the active Worker deployment is version `f26e000b-532c-44cd-8de3-cd04aab40fb2` at 100%, and historical version `906ea1c8-e2cd-42d4-915f-db8d0f8ebe56` is a same-Worker prior deployment suitable for rollback review.
  Evidence: `npx wrangler whoami`, `npx wrangler deployments list --name hyperopen --json`, `npx wrangler versions view 906ea1c8-e2cd-42d4-915f-db8d0f8ebe56 --name hyperopen --json`, DNS lookups, and HTTPS probes on 2026-08-05. Public matrix: apex `200`, Testnet `200`, Mainnet `503`/`no-store`, status `200`/`no-store`; Testnet health `200 {"status":"ok"}`, Testnet same-network proxy `200`, Testnet cross-network proxy `404`.
- Observation: the technical Mainnet candidate is now locally reproducible, but the production Wrangler config remains Testnet-only by design.
  Evidence: the candidate dry-run binds `HYPERUNIT_MAINNET_URL` and `HYPERUNIT_TESTNET_URL` with `HYPEROPEN_MAINNET_ENABLED="true"`, while `wrangler.jsonc` still binds only `HYPERUNIT_TESTNET_URL`; no publish command was run.
- Observation: the current release checks are green after the Close All, theme-token, and Trusted Types repairs.
  Evidence: with JDK 21 and a temporary Node localStorage file, the complete matrix passed `35/35`, including `5882` ClojureScript tests / `32817` assertions and `561` websocket tests / `3184` assertions; focused Close All Playwright passed `4/4` and the rebuilt candidate dry-run exited `0`.
- Observation: the white-label browser contract defaults to an example tenant's institutional theme and disabled `/trade` route.
  Evidence: the first two DEXHelm Playwright invocations failed only because their environment omitted `HYPEROPEN_EXPECT_THEME=dark` and an empty `HYPEROPEN_EXPECT_DISABLED_ROUTE`; the rerun with DEXHelm's actual contract passed all four viewports.
- Observation: the deterministic white-label Playwright suite is green while the broader Browser MCP design-system review is not.
  Evidence: `/Users/zh/Documents/Hyperopen/tmp/browser-inspection/dexhelm-mainnet-opening-qa/browser-report.json` records `FAIL` for the Mainnet candidate at all governed widths because visible text contains no `Mainnet` indicator; computed `[data-role='header-brand-name']` `letter-spacing` is `-0.72px` at 375px and `-1.44px` at 1440px.
- Observation: the prior Browser MCP UX blockers are resolved in the rebuilt candidate; Chromium reports authored zero tracking as `normal` through CSSOM, which the dedicated contract normalizes to the approved zero token.
  Evidence: `npm run test:playwright:mainnet-opening` passed `8/8` at all four governed widths after the network banner and brand tracking changes; the local-only logo CSP warning is emitted by Wrangler's port-bearing dev origin and is explicitly excluded from the local console-error assertion, while production CSP remains canonical-origin scoped.
- Observation: the public operator page already includes risk disclosure, source/AGPL links, and an explicit unavailable affiliate state, but no privacy-policy or terms-of-service entry was found in the Worker page or DEXHelm tenant configs.
  Evidence: `workers/hyperopen-worker.mjs` contains the risk/source/license copy; `config/white-label/dexhelm.json` and `config/white-label/dexhelm-mainnet.json` set affiliate status to `unavailable`; repository search for `privacy` and `terms` found no DEXHelm public-page links. A legal owner must supply and approve those materials before Phase 8 signoff.
- Observation: the live apex page confirms the same legal-link gap.
  Evidence: a read-only scan of `https://dexhelm.com/` found `AGPL`, `Risk`, and `Source`, but no `Privacy` or `Terms` link. The Mainnet response remains Cloudflare-served `503` with `cache-control: no-store`.
- Observation: the repository now contains an outside-Cloudflare public probe and scheduled workflow, but alert ownership is still incomplete and the workflow is not active until explicitly pushed.
  Evidence: `tools/cloudflare/public_monitor.mjs`, `.github/workflows/dexhelm-public-monitor.yml`, and the monitoring runbook cover apex, Testnet, expected Mainnet 503, status, and health; a real read-only run returned `200/200/503/200/200`. No alert recipient, incident channel, or independent outage communication owner is recorded.
- Observation: operational and legal readiness remain blocked independently of the technical release checks.
  Evidence: the repository probe and read-only rollback drill now pass, but no named monitor alert recipient, incident owner/on-call route, independent outage-communication path, or legal practice profile is available to approve DEXHelm privacy and Terms materials. The existing Hyperliquid legal pages are reference-only and cannot satisfy DEXHelm approval.
- Observation: the maintainer supplied Hyperliquid's public legal pages as references, but those documents cannot be reused as DEXHelm terms without legal review.
  Evidence: `https://hyperfoundation.org/termsOfService` (revised 2024-10-10) covers eligibility, prohibited jurisdictions, arbitration/class waiver, service disclaimers, and third-party wallet responsibility; `https://hyperfoundation.org/privacyPolicy` (revised 2024-10-10) covers wallet/IP/log data, cookies/analytics, third-party wallets, data rights, and controller responsibilities. DEXHelm still needs its own approved texts and links.
- Observation: the Positions Close All path is now implemented through a dedicated confirmation, batch planner, and lifecycle effect.
  Evidence: deterministic/formal tests pass, and focused Playwright at `1280` and `1440` proves explicit confirmation followed by exactly one two-leg opposite-side, absolute-size, reduce-only IOC batch; `375` and `768` prove no mobile affordance.
- Observation: the repository already has a state-driven destructive-confirmation reference, but it cannot be reused as the positions implementation.
  Evidence: `src/hyperopen/order/actions.cljs` and `src/hyperopen/views/account_info/tabs/open_orders.cljs` own the Open Orders cancel-visible confirmation state, dismiss controls, and responsive anchored/centered presentation. Position closure needs different source data, order construction, reduce-only invariant, and settlement lifecycle, so it must receive a separate `:positions-ui` confirmation model rather than sharing cancel-order state.
- Observation: per-position market-close construction is already pure and reduce-only, but a mass-close merger must be stricter than concatenating arbitrary requests.
  Evidence: `hyperopen.account.history.position-reduce/from-position-row` plus `prepare-submit` resolves market identity and produces an order whose wire `:r` is `true`; each resulting action can carry root-level builder metadata. A mass request must fail before network I/O unless every current snapshot row prepares successfully and optional `:builder` maps are all absent or exactly equal.
- Observation: the initial Close All Playwright failures were fixture defects and are now resolved without weakening the production memory-only key policy.
  Evidence: the fixture now uses `:session` mode and the process-local agent lockbox instead of persisted local plaintext. Focused Playwright passes `4/4`, and the signed exchange boundary remains simulated.
- Observation: the full post-repair gate matrix and UI policy checks are green.
  Evidence: `npm run gates` passed `35/35`; the three modal colors use governed tokens, and the governed six-pass browser review passed at `375/768/1280/1440` with zero issues.
- Observation: the authorized `1 USDC` Perps/Spot round trip now has independent Hyperliquid ledger evidence, but it is not an Arbitrum ERC-20 round trip.
  Evidence: `userNonFundingLedgerUpdates` records matching `accountClassTransfer` entries with `toPerp: false` and `toPerp: true` approximately 113 seconds apart; Arbitrum Sepolia has no receipts for those internal ledger hashes, as expected.
- Observation: the minimum Testnet withdrawal remains blocked by an upstream Bridge2 rejection rather than an ambiguous or duplicated submission.
  Evidence: two manually confirmed `5 USDC` attempts and a later separately confirmed `10 USDC` attempt returned `Error withdrawing from bridge`; post-attempt ledger queries were empty, withdrawable Perps balance stayed `977.563573 USDC`, no payout transaction appeared, general Testnet smoke passed, Bridge2 had ample USDC2, and the request/signing shape matches the current official SDK.
- Observation: the earlier operator-network classification was `BlockActions`, but it changed before the latest failure.
  Evidence: Testnet and Mainnet `legalCheck` changed from historical `restrictions: "a"` to `restrictions: "o"` for both the connected account and zero address before the `10 USDC` attempt. DEXHelm's fail-closed preflight remains required for future blocked or unavailable decisions, but the old classification is not the current cause of the bridge error.
- Observation: Bridge2 was not inactive for the entire day, but its last observed successful USDC2 payout preceded the latest attempt by nearly four hours.
  Evidence: Arbitrum Sepolia transaction `0x903b855ece52263f23542e16b8e2ebda120b65047151df13c32d9d8747369cf6` succeeded at `2026-08-06 14:38:22 CST` and transferred `9 USDC2` from Bridge2; no later Bridge2 payout was present through the `18:26 CST` read-only scan. This supports an upstream Testnet bridge availability or account-processing failure during the attempt window, not a proven DEXHelm request-shape defect.
- Observation: the Bridge2 UI's current withdrawal facts are inconsistent with the official current source set.
  Evidence: `src/hyperopen/funding/domain/assets.cljs` defines `withdraw-min-usdc` as `5`, `src/hyperopen/funding/domain/preview.cljs` rejects USDC below that value, and `src/hyperopen/funding/application/modal_vm/amounts.cljs` falls back to `~10 seconds` and `None` for the Bridge2 path. Official exchange documentation updated 2026-07-31 says the `withdraw3` fee is `$1` and finalization is about five minutes; the official Bridge2 page says `3-4 minutes`; official documentation's explicit `5 USDC` minimum is for deposits only.
- Observation: the official source set gives two close but non-identical time estimates for the same Bridge2 withdrawal.
  Evidence: the exchange docs describe finalization as about five minutes while the Bridge2 page describes `3-4 minutes`; the user-facing contract uses the honest combined range `3-5 minutes` instead of inventing a single exact duration or silently choosing one source.
- Observation: the final Mainnet candidate originally failed to give its public fingerprinted JavaScript the immutable cache rule that the sibling Testnet asset received; the release-boundary repair now resolves the mismatch.
  Evidence: the failing local Wrangler probe returned `Cache-Control: public, max-age=0, must-revalidate` for the Mainnet fingerprinted script while Testnet returned `public, max-age=31556952, immutable`. The candidate builder now emits `22` exact `/__hyperopen_mainnet/css|js/` rules in the authoritative root `_headers`; the dedicated Mainnet/Testnet Playwright matrix reads document-discovered CSS/JavaScript responses and passes `8/8` with the exact immutable value on both hosts.

## Decision Log

- Decision: keep `app.dexhelm.com` closed with HTTP 503 for the entire duration of this plan until Phase 8 is signed and Phase 9 is separately authorized.
  Rationale: the zero-to-live runbook treats Mainnet opening as a product/risk-policy change and forbids inferring it from a successful deploy or Testnet approval.
  Date/Author: 2026-08-05 / Codex, from direct maintainer request.
- Decision: treat `f26e000b-532c-44cd-8de3-cd04aab40fb2` as the supplied current Worker version, not as proven rollback evidence.
  Rationale: the id is durable context from the maintainer but is absent from checked-in records; Wrangler must confirm Worker identity and select a prior verified version before any mutation.
  Date/Author: 2026-08-05 / Codex.
- Decision: preserve the Testnet-only proxy boundary in `wrangler.jsonc` and the Worker until a Phase 9 change is approved.
  Rationale: only the explicitly authorized Mainnet release may add Mainnet delivery; generic, lookalike, and Mainnet proxy paths must remain rejected before then.
  Date/Author: 2026-08-05 / Codex.
- Decision: interpret the 2026-08-06 Worker deployment authorization as permission to publish the reviewed existing-Worker update through `wrangler.jsonc`, while refusing to use `wrangler.mainnet-opening.jsonc` until the unresolved Phase 7/8 blockers are cleared.
  Rationale: the zero-to-live runbook permits a Testnet-only deployment when withdrawal settlement is blocked, but treats incomplete Testnet settlement, monitoring/incident ownership, and compliance review as non-negotiable blockers to Mainnet opening.
  Date/Author: 2026-08-06 / Codex.
- Decision: treat the official Bridge2 and exchange estimates as a combined `3-5 minutes` user-facing range and label the Bridge2 charge as a `1 USDC` withdrawal fee.
  Rationale: the two official pages are both current and differ by roughly one minute; a range preserves both facts and avoids false precision. The charge is a protocol withdrawal fee, so it must not continue to render as `None` or be mislabeled as an unrelated destination-chain fee.
  Date/Author: 2026-08-06 / Codex spec_writer.
- Decision: remove the hardcoded USDC withdrawal threshold while retaining a positive-amount guard and the separate `5 USDC` deposit minimum.
  Rationale: current official exchange documentation does not define a USDC withdrawal minimum, while the endpoint accepts an amount string and documents an example of `"1"`. The client should accept any finite amount greater than zero up to the withdrawable balance and let the exchange remain authoritative for any dynamic rejection; deposit validation remains unchanged at `5 USDC`.
  Date/Author: 2026-08-06 / Codex spec_writer.
- Decision: preserve the existing `withdraw3` action and EIP-712 signing schema exactly.
  Rationale: the current official Python SDK 0.24.0 still uses destination, amount string, time, signatureChainId, and hyperliquidChain, with top-level nonce equal to time. This task is display and validation parity, not a request-shape migration.
  Date/Author: 2026-08-06 / Codex spec_writer.
- Decision: treat the historical bundle warning as remediated only after rebuilding the current release workload, and keep the 640,000-byte budget unchanged.
  Rationale: the current DEXHelm branded and Mainnet candidate modules measure 635,653 gzip bytes; changing the budget would hide the fact that the current artifact already fits.
  Date/Author: 2026-08-05 / Codex.
- Decision: treat the Browser MCP design-system `FAIL` as a release blocker until both Mainnet environment visibility and zero letter-spacing are corrected and rechecked.
  Rationale: a user-facing Mainnet terminal must make the active network unmistakable, and the repository UI contract requires zero letter-spacing; a green deterministic smoke suite does not waive either issue.
  Date/Author: 2026-08-05 / Codex.
- Decision: preserve `:actions/trigger-close-all-positions` as the public entry point, retain its zero-argument call shape, and add only an optional trigger-bounds argument for dialog placement.
  Rationale: the visible header already dispatches the zero-argument action. Keeping that path prevents an unrelated caller regression, while bounded placement can reuse the established desktop anchored-dialog and smaller-screen centered fallback behavior.
  Date/Author: 2026-08-05 / Codex.
- Decision: make a fresh, unfiltered position snapshot the authority for Close All, and require exact snapshot identity before any order request is emitted.
  Rationale: a table's search, sort, or visible rows are presentation concerns and can omit live exposure. A snapshot containing each position key, coin, perp DEX, and signed size detects data changes between confirmation and submission, preventing an order based on stale direction or quantity.
  Date/Author: 2026-08-05 / Codex.
- Decision: submit Close All through a dedicated effect and lifecycle state rather than the generic `:order-form-runtime` or one effect per position.
  Rationale: Close All is a distinct destructive batch whose user-visible state must remain `:confirming`, `:submitting`, `:success`, or `:error`. A single batch boundary makes duplicate-prevention, all-or-nothing preflight, accepted/rejected accounting, and post-submit refresh observable and testable.
  Date/Author: 2026-08-05 / Codex.
- Decision: do not add a mobile Close All affordance in this repair; hide the existing desktop affordance and its confirmation in read-only, spectate, and empty/all-zero states.
  Rationale: the defect is the rendered desktop control that currently does nothing. Expanding destructive reachability onto mobile is not needed to repair it, while both the renderer and action guard must prevent a stale or programmatic invocation from mutating a read-only account.
  Date/Author: 2026-08-05 / Codex.
- Decision: seed the Close All browser regression through the current process-local `:session` agent contract rather than the deprecated persisted-local plaintext path.
  Rationale: production signing intentionally rejects local plaintext recovery. The lockbox fixture preserves that security boundary while deterministically exercising the one signed batch submission through the exchange simulator.
  Date/Author: 2026-08-06 / Codex.
- Decision: keep Phase 8 blocked while the legal practice profile, named monitor alert recipient, named incident owner/channel, independent outage-communication path, and complete Testnet settlement remain unavailable.
  Rationale: a green local test matrix and read-only rollback drill cannot substitute for legal approval, accountable external alerting and incident response, or independently verified withdrawal settlement.
  Date/Author: 2026-08-06 / Codex.
- Decision: treat the Mainnet fingerprinted-asset cache mismatch as a release blocker and constrain its repair to candidate release assembly, generated static-header rules, and deterministic verification.
  Rationale: a content-hashed JavaScript URL may safely receive an immutable cache lifetime only when the candidate root header policy recognizes the Worker-internal `__hyperopen_mainnet` asset path. The observed document cache policy forces unnecessary revalidation and proves the two host releases are not equivalent. The Worker host/proxy logic, `src/**`, tenant network choice, wallet flows, real-fund actions, and Cloudflare deployment are outside this repair.
  Date/Author: 2026-08-06 / Codex.

## Outcomes & Retrospective

The initial planning milestone and the local technical candidate are substantially complete. The repository has an isolated Mainnet candidate with deterministic tests, repeated builds, artifact preflight, a current `35/35` gate matrix, `5888` ClojureScript tests / `32843` assertions, and a fresh dedicated `8/8` Mainnet/Testnet four-viewport contract on the final candidate. The governed trade-route browser review and focused Close All matrix also pass at all four widths, the read-only rollback drill identifies exact current and prior Worker versions, the repaired root header policy gives both hosts' fingerprinted CSS/JavaScript the exact immutable cache value, and the current branded candidate is below the bundle target at `635,653` gzip bytes. A Testnet-only production Worker update is now live as version `ce68c039-4f88-4640-ad4d-9271a1a3e48a`; its public matrix and proxy isolation pass. Mainnet is still not opened: `app.dexhelm.com` remains `503`, and no DNS, wallet, signature, or fund state changed. The remaining work is external or owner-dependent: complete Testnet withdrawal settlement evidence, named monitoring/alerting and incident ownership, an independent outage-communication path, legal-practice-profile-backed Privacy/Terms review, complete the Bridge2 source-parity UI milestone, and final unconditional Phase 8 readiness. The parity milestone is intentionally low-complexity display/validation work: it removes one unsupported threshold and replaces hardcoded fallback facts, without changing the exchange request or signing boundary.

The reproduced Positions Close All no-op is repaired. The worker-owned path has destructive confirmation, explicit request state, failure recovery, all-or-nothing batch safety, and exact reduce-only IOC assertions. Deterministic/formal validation, focused Playwright `4/4`, theme policy, and governed browser QA all pass. The release Trusted Types policy also now permits only the three checked-in worker paths and rejects path/query variants. No wallet, live exchange, or Mainnet release action was invoked.

## Context and Orientation

Hyperopen is a ClojureScript browser client. `config/white-label/dexhelm.json` is the checked-in DEXHelm tenant declaration and selects `testnet` at compile time for the public artifact. `tools/cloudflare/build_dexhelm_release.mjs` builds `out/white-label/dexhelm`, rewrites only generated HyperUnit URLs, refreshes tenant digests, and verifies the artifact. `wrangler.jsonc` uploads that directory to Worker `hyperopen` with `workers_dev` disabled and exposes only `HYPERUNIT_TESTNET_URL=https://api.hyperunit-testnet.xyz`. `workers/hyperopen-worker.mjs` owns the host policy: apex and status pages are 200, Testnet terminal/proxy are available, Mainnet app is a 503 closed page, and generic/Mainnet proxy paths are 404. The source default in `shadow-cljs.edn` is not public-release authority; only the tenant release declaration and Worker host policy can permit a production network.

The Phase 9 candidate is assembled separately by `tools/cloudflare/build_dexhelm_mainnet_opening_candidate.mjs`. It copies the Testnet release to `out/cloudflare/dexhelm-mainnet-opening/` and the Mainnet release below `__hyperopen_mainnet/`, then creates `mainnet-opening-manifest.json` from the final artifact inventory. When `HYPEROPEN_MAINNET_ENABLED` is true, `workers/hyperopen-worker.mjs` internally rewrites a public Mainnet asset request such as `/js/main.<hash>.js` to `/__hyperopen_mainnet/js/main.<hash>.js` before it reaches the static-asset binding. `_headers` is the generated Cloudflare static-header policy at the candidate root. A `_headers` file inside the nested Mainnet directory is an artifact, not a root-level rule for that internal asset path. Therefore the root file must contain the exact immutable cache entries for the prefixed fingerprinted Mainnet CSS and JavaScript paths, while document, metadata, service-worker, and non-fingerprinted worker-script paths retain `public, max-age=0, must-revalidate`.

The four public domains are `https://dexhelm.com` (apex/product), `https://testnet.dexhelm.com` (Testnet terminal), `https://app.dexhelm.com` (Mainnet terminal, currently closed), and `https://status.dexhelm.com` (status page). The current Worker version supplied for this record is `f26e000b-532c-44cd-8de3-cd04aab40fb2`; verify it against the intended Cloudflare account before relying on it. The source baseline is the clean `main` branch at `3289a95a969373e863f1955f8de919c27914372e`, from `https://github.com/thegeronimo/hyperopen.git` (AGPL-3.0, with the fork remote also present).

The Positions tab has a separate, currently broken, user path. `src/hyperopen/views/account_info/tabs/positions/desktop.cljs` renders `Close All` and dispatches `:actions/trigger-close-all-positions`; `src/hyperopen/runtime/collaborators/account_history.cljs` resolves that action to `src/hyperopen/account/history/position_overlay_actions.cljs`, where it currently returns no effects. The future repair must follow the existing per-position close builder in `src/hyperopen/account/history/position_reduce.cljs`, not invent a second order shape. A position snapshot is a vector of current nonzero positions across the default clearinghouse and each named perp DEX; each entry must retain its stable position key, coin, DEX, and signed `:szi` size. A close order has the opposite side of that signed size, its absolute quantity, market immediate-or-cancel behavior, and reduce-only wire flag `:r true`. A batch is valid only when every snapshot entry produces such an order and every optional root builder-fee map is absent or exactly the same; otherwise the batch is rejected before any network I/O.

The modal-like confirmation may follow the interaction and responsive placement reference in `src/hyperopen/order/cancel_visible_confirmation.cljs` and `src/hyperopen/views/account_info/tabs/open_orders.cljs`, but it must own separate state at `[:positions-ui :close-all-confirmation]`. It must never write to `:order-form-runtime` or the Open Orders cancel-confirmation path. `src/hyperopen/order/effects.cljs`, `src/hyperopen/runtime/effect_adapters/order.cljs`, `src/hyperopen/runtime/effect_adapters.cljs`, and `src/hyperopen/app/effects.cljs` are the current effect-to-runtime path; a future dedicated Close All effect must be registered along that complete route and covered by `src/hyperopen/runtime/effect_order_contract.cljs` before it can perform network work.

## Plan of Work

### Milestone 0: Freeze scope and authority

Keep this file under `docs/exec-plans/active/` and do not create a second requirements document. The only present-session edit is this ExecPlan. This spec-writer pass must not edit `/Users/zh/Documents/Hyperopen/src/**` or tests. The future Close All implementation is limited to the reviewed Positions, order-effect, schema/registration, and deterministic test surfaces named in this plan and must be performed by the `worker` role; it does not authorize wallet-extension automation, a real order, DNS changes, or a Cloudflare publish. Any future release/Worker boundary change remains separately limited by its approval boundary.

Acceptance is observable when `git status --short --branch` reports clean `main`, `git rev-parse HEAD` prints the recorded commit, and `app.dexhelm.com` still returns 503.

### Milestone 1: Reconfirm source, account, domains, and rollback target

Record non-secret provenance and Cloudflare identity. Confirm the account and Worker name with `npx wrangler whoami`; list versions with `npx wrangler versions list --name hyperopen --json`; and resolve all four hosts with `dig`. Select a prior verified version as the rollback target only after its Worker identity, host matrix, and release evidence match. Historical candidate IDs are `906ea1c8-e2cd-42d4-915f-db8d0f8ebe56`, `588a4869-597b-4fd0-b744-42a631b7ec6f`, `22f0460a-f31c-4b52-8f44-038d5afffec0`, `2713e467-dd3b-4e29-b320-b338937a8381`, and `9e842a8f-8eb6-412f-a105-c5d15df6da66`; they are leads, not approval.

Acceptance is a redacted record containing account/zone/Worker identity, certificate and DNS state, current version `f26e000b-532c-44cd-8de3-cd04aab40fb2`, one verified prior rollback version, and the complete host matrix. Never write tokens, cookies, wallet addresses, signatures, or response bodies into this file.

### Milestone 2: Prove the deterministic Testnet release boundary

Run the release validators, build adapter, artifact preflight, local Worker, and static/proxy verifiers. Confirm generated JavaScript contains only the fixed Testnet same-origin proxy base, no direct HyperUnit origin, and no Mainnet proxy authority; confirm source defaults remain untouched. Confirm `app.dexhelm.com` documents, assets, and proxy paths stay closed, while Testnet document navigation has exactly one `hyperliquidNetwork=testnet` parameter.

Acceptance is the command sequence in `Concrete Steps` completing with zero failures and observable responses: apex 200, Testnet `/trade` 200, status 200, `/api/health` 200 JSON with `no-store`, Mainnet app 503, Mainnet/generic proxy paths 404, and unknown routes 404. Any unexplained preflight, browser, local Worker, or dry-run failure blocks the next milestone.

### Milestone 3: Complete Phase 7 Testnet fund-flow acceptance

Obtain separate user approval for the disposable wallet address and maximum amounts. The user manually confirms every wallet signature and captcha. Follow `docs/runbooks/hyperliquid-testnet-implementation.md` exactly: explicit `?hyperliquidNetwork=testnet` URL; Arbitrum Sepolia (`421614`); current USDC2 `0x1baAbB04529D43a73232B713C0FE471f7c7334d5`; Bridge2 `0x08cfc1B6b2dCF36A1480b99353A354AA8AC56f89`; deposit and clearinghouse credit; fresh API-wallet enable and `extraAgents`; minimal order then complete close with empty positions/open orders; Perps -> Spot -> Perps with both ledger checks; minimum withdrawal with both Perps debit and Sepolia ERC-20 `Transfer` delivery.

Acceptance requires redacted API/chain evidence for each step, requested and delivered amounts, transaction hashes, and any fee discrepancy. A successful transaction receipt, a page toast, or a submitted withdrawal without settlement is insufficient. Stop on address, network, payload, amount, destination, or pending-settlement mismatch; record `BLOCKED` rather than substituting Mainnet or an obsolete same-symbol token.

### Milestone 4: Establish production operations and compliance readiness

Assign named owners and attach evidence for external monitoring (outside Cloudflare), alert routing, incident classification and independent outage communication, support, rollback, production approval, risk disclosure, privacy/terms, AGPL/source obligations, and affiliate status/disclosure. The external monitor must check the Worker health endpoint, apex, Testnet route, status route, and expected Mainnet 503. Confirm telemetry and logs exclude wallet secrets, signatures, authorization headers, cookies, and sensitive browser storage. Rebuild and measure the release workload before changing the bundle budget; the current branded candidate is already below the unchanged target.

Acceptance is a signed Phase 8 checklist naming every owner, monitor URL and last probe, incident channel, rollback version and read-only drill result, compliance review state, affiliate state, and current bundle measurement. The in-Worker status page alone does not satisfy independent monitoring.

### Milestone 5: Run full release gates and obtain Phase 8 signoff

Execute the exact safe-order command sequence below on the release candidate. Let Playwright exit cleanly and run `npm run browser:cleanup` only for sessions created during this work. The production owner then signs the Phase 8 checklist; this signature is a human boundary and cannot be inferred from green tests, a deployed Worker, or Testnet success.

Acceptance is a recorded PASS for deterministic Node/release tests, white-label Playwright at 375/768/1280/1440, rebuilt artifact preflight, local Wrangler checks, `npm run cloudflare:check`, and an all-PASS gate matrix, plus the signed operations/compliance checklist. Mainnet remains 503 while this evidence is gathered.

### Milestone 5A: Repair the Positions Close All safety defect

This is the smallest safe future implementation of the reproduced desktop Positions defect. It is a worker-owned source and test change; the present documentation-only pass must not materialize it. Its scope is the existing desktop header action, its local confirmation state, construction and submission of reduce-only closing orders, and the explicit lifecycle the user sees. It must not add a mobile Close All button, change how the table filters/sorts rows, share Open Orders cancellation state, alter the normal order-form lifecycle, automate a wallet confirmation, or send an order during tests.

First create `src/hyperopen/account/history/close_all_positions.cljs` as a pure owner for three operations: deriving the current unfiltered nonzero snapshot from the default and named-perp-DEX clearinghouse data; making the confirmation model from that snapshot and optional trigger bounds; and preparing one valid batch request from a freshly rederived snapshot. Use the existing `position-identity` and `position-reduce/from-position-row` plus `position-reduce/prepare-submit` path for every leg. Reject the complete operation when a row is not a numeric nonzero signed `:szi`, a market or asset is missing, a prepared leg is not reduce-only, its side does not oppose the original signed size, its size is not the absolute original size, its request is not market IOC, its wire order lacks `:r true`, or builder metadata differs across legs. Do not submit the valid subset. The result must identify the reason in user-facing language and emit no network effect.

Next replace the no-op in `src/hyperopen/account/history/position_overlay_actions.cljs` with a state transition that preserves `trigger-close-all-positions` and adds actions to dismiss the confirmation, handle Escape, and submit it. The trigger must calculate the complete current snapshot without table filters and save `[:positions-ui :close-all-confirmation]` atomically as `:confirming`; it must emit no order effect. The confirmation stores the snapshot identity and the trigger bounds. The submit action must reject read-only or spectate state, empty/all-zero input, a non-confirming lifecycle, and a changed snapshot. On a changed snapshot it keeps a visible error such as `Positions changed. Review current positions before closing.` and emits no order. On valid preparation it atomically writes `:submitting`, clears any prior error, disables conflicting commands, and only then emits one `:effects/api-submit-close-all-positions` batch effect. Repeated clicks, Enter, Escape, backdrop clicks, and programmatic action calls while `:submitting` must not emit another batch.

Implement the dedicated effect in `src/hyperopen/order/effects.cljs` and expose/register it through `src/hyperopen/runtime/effect_adapters/order.cljs`, `src/hyperopen/runtime/effect_adapters.cljs`, and `src/hyperopen/app/effects.cljs`. It may reuse the established signed order API and account mutation target, but it owns `[:positions-ui :close-all-confirmation]` through the entire response. It must report `Closing N positions...` while in flight, move to `:success` only when every leg is accepted, and then refresh account position/open-order/history surfaces without claiming the account is flat before refreshed data proves it. A transport failure, exchange failure, unknown response, or any partial acceptance must move to `:error`, keep the exact accepted/rejected count and actionable message visible, and re-enable a safe dismissal/review path. No retry is automatic. The generic `:order-form-runtime` must remain unchanged by this effect.

Update `src/hyperopen/account/history/actions.cljs`, `src/hyperopen/runtime/collaborators/account_history.cljs`, `src/hyperopen/schema/contracts/action_args.cljs`, `src/hyperopen/schema/runtime_registration/trade.cljs`, and `src/hyperopen/runtime/effect_order_contract.cljs` so all action and effect identifiers are valid. The existing zero-argument trigger remains valid; its optional map argument is limited to numeric dialog bounds. The submit action's policy must require a projection before the one dedicated heavy I/O effect and forbid duplicate heavy effects.

Update `src/hyperopen/views/account_info/vm.cljs`, `src/hyperopen/views/account_info/tabs/positions.cljs`, and `src/hyperopen/views/account_info/tabs/positions/desktop.cljs` to consume this model. Show the destructive desktop trigger only when the account is writable, not spectating, and the unfiltered snapshot has at least one nonzero position. When visible, activation opens an accessible destructive confirmation that identifies the number of positions and provides equally reachable `Cancel` and destructive `Close all positions` controls. Support button activation, keyboard focus, Escape, backdrop dismissal, and the existing anchored desktop/centered fallback presentation while confirming or showing an error; while submitting, show the lifecycle text and disable both destructive repeat submission and dismissal controls that would imply cancellation. Do not render the trigger or confirmation at 375 or 768 pixels, and do not let it overflow at any governed width.

Acceptance is observable before Mainnet can progress: at 1280 and 1440 pixels a writable account with two current positions opens a confirmation before any submit request; the confirmation lists the count; confirm emits exactly one batch containing two opposite-side, absolute-size, reduce-only IOC orders; the request leaves each position's DEX and market identity intact; and accepted response state reads `Close requests submitted for 2 positions` before refresh. Changing one size or direction after opening the confirmation produces the stale-data error and zero requests. One invalid leg or a builder mismatch produces a clear error and zero requests. A partial response displays accepted/rejected counts as an error. Read-only, spectate, and zero-only accounts render no trigger or dialog and their action handlers emit no order. At 375 and 768 pixels there is no newly reachable Close All control or layout overflow. Every one of these states has deterministic ClojureScript coverage and the stable user path has committed Playwright coverage at 375, 768, 1280, and 1440 pixels.

### Milestone 5B: Preserve immutable caching for Mainnet fingerprinted assets

Repair only the candidate release-generation/header boundary. In `tools/cloudflare/build_dexhelm_mainnet_opening_candidate.mjs`, after both tenant releases are copied to the staging candidate and before the artifact inventory and `mainnet-opening-manifest.json` are calculated, derive the Mainnet release's already-approved fingerprinted immutable asset paths through the shared release-header policy rather than introducing a second filename matcher. Materialize matching root `_headers` entries for the Worker-internal paths `/__hyperopen_mainnet/css/<fingerprinted-css>` and `/__hyperopen_mainnet/js/<fingerprinted-js>`. Each added entry must first detach the document `Cache-Control` header and then set exactly `public, max-age=31556952, immutable`. Preserve existing root Testnet header blocks unchanged. Do not give immutable cache policy to `/__hyperopen_mainnet/js/module-loader.json`, `release-route-metadata.js`, `site-metadata.json`, `sw.js`, un-fingerprinted worker scripts, documents, health, or proxy responses.

The implementation may make the shared header builder in `tools/release-assets/security_headers.mjs` expose the smallest pure helper needed to prefix its canonical immutable asset rules. It must not parse or hand-edit generated `_headers` text, duplicate the existing fingerprint classification, alter `workers/hyperopen-worker.mjs`, change any `src/**` file, change the two tenant releases' JavaScript, or add a deploy command. The candidate manifest must be generated only after the root `_headers` file is final so its digest inventory remains exact.

Extend deterministic release tests with a focused temporary candidate fixture under `tools/cloudflare/`, reusing the existing candidate-assembly dependency injection. The fixture must prove that every declared Mainnet fingerprinted CSS/JavaScript path produces one corresponding root-prefixed immutable rule, that the Testnet root rule is unchanged, and that un-fingerprinted/control assets have no Mainnet immutable rule. It must also prove `verifyDexhelmMainnetOpeningCandidate` accepts the assembled candidate after the root header file is changed and rejects a candidate whose declared artifact digest does not match the finalized header file. Extend `tools/playwright/test/dexhelm-mainnet-opening.spec.mjs` without adding wallet or exchange activity so each existing host/viewport case reads the document's main-script response and asserts the exact immutable policy for both Mainnet and Testnet. The existing suite should remain `8/8`; a status-code-only check is not sufficient.

Acceptance is observable only when local Wrangler with `wrangler.mainnet-opening.jsonc` and `--local-upstream app.dexhelm.com` returns `200` Mainnet `/trade`, `200` JSON for `/api/health` and `/api/hyperunit/mainnet/v2/estimate-fees`, and `404` for cross-network, generic, and unknown proxy paths, while the fingerprinted public Mainnet CSS and JavaScript named by that document each return exactly `Cache-Control: public, max-age=31556952, immutable`. The sibling Testnet assets must return the same immutable value. Candidate documents and control assets must remain `public, max-age=0, must-revalidate`; health stays `no-store`; direct requests to the internal prefix remain rejected by the Worker. This milestone is local-only: it does not publish, upload, sign, transfer, withdraw, or place an order.

### Milestone 5C: Align Bridge2 USDC withdrawal facts with current official sources

This milestone repairs the funding domain and modal view model that currently present the Bridge2 USDC withdrawal as a `5 USDC` minimum, `~10 seconds`, and no fee. In `src/hyperopen/funding/domain/assets.cljs`, keep `deposit-min-usdc` at `5` but remove the USDC withdrawal minimum from the validation path; do not alter HyperUnit asset-specific withdrawal minimums. Preserve any existing exported `withdraw-min-usdc` compatibility binding if callers require it, but make it non-authoritative and ensure standard USDC preview and summary code never reads it. In `src/hyperopen/funding/domain/preview.cljs`, reject non-finite or non-positive amounts and amounts above the available balance, but accept a finite positive USDC amount such as `1` without a minimum-threshold error. In `src/hyperopen/funding/application/modal_vm/amounts.cljs` and its presentation/model adapters, make the Bridge2 path expose `Estimated time` as `3-5 minutes` and `Withdrawal fee` as `1 USDC`, while retaining live HyperUnit estimates and chain-fee wording for address-based assets. Preserve the existing `withdraw3` action map and signing path byte-for-byte in shape.

Update the focused ClojureScript tests under `test/hyperopen/funding/domain/policy_preview_test.cljs`, `test/hyperopen/funding/actions_test.cljs`, `test/hyperopen/funding/application/modal_vm/amounts_test.cljs`, `test/hyperopen/funding/application/modal_vm_test.cljs`, and `test/hyperopen/views/funding_modal_test.cljs` (or the smallest existing owners selected by the test writer). The tests must prove that `1 USDC` produces the existing `withdraw3` action, `0.000001 USDC` is not rejected by a fabricated minimum, `0` and malformed amounts remain invalid, `5 USDC` deposit validation still rejects `1` and accepts `5`, and the Bridge2 summary contains exactly `Estimated time / 3-5 minutes` and `Withdrawal fee / 1 USDC` with no `Minimum withdrawal` row. HyperUnit tests must retain their current minimum, live ETA, and live fee behavior.

Extend the existing funding-modal Playwright regression in `tools/playwright/test/trade-regressions.spec.mjs` using the current `HYPEROPEN_DEBUG` bridge and simulator. Open the withdraw modal, select USDC, and assert the two official-source rows and absence of a USDC minimum row at the governed desktop and mobile widths used by the test suite. Enter a positive sub-5-USDC amount and assert the local preview does not show a minimum error; do not invoke a wallet, signature prompt, live exchange call, or real withdrawal. Run the focused Playwright test before the full gates and complete the six-pass browser-QA matrix at `375`, `768`, `1280`, and `1440`.

### Milestone 6: Phase 9 Mainnet opening (separate authorization required)

Create or refresh a separate Phase 9 ExecPlan describing the smallest reviewed change that opens `app.dexhelm.com`, adds deterministic Mainnet release/network tests, preserves forced Testnet behavior, and keeps proxy boundaries explicit. Do not implement or deploy that change in this planning session. Before any external mutation, obtain explicit current-session authorization for the Phase 9 deploy; deployment authorization does not authorize a wallet connection, signature, transfer, deposit, withdrawal, or order.

Acceptance after an authorized Phase 9 release is public and technical only: `app.dexhelm.com` returns the approved Mainnet document and its Mainnet proxy behavior, `testnet.dexhelm.com` remains forced to Testnet, all four host statuses/headers/health/logo checks pass, and the new Worker version and prior rollback version are recorded. Real-fund Mainnet activity requires a separate action-and-amount authorization.

## Concrete Steps

Run every command from `/Users/zh/Documents/Hyperopen`. Commands up through Phase 8 are read-only or local-build operations unless explicitly marked as a human approval or publish boundary.

    npm run setup:worktree
    git remote get-url origin
    git remote get-url fork
    git rev-parse HEAD
    git branch --show-current
    git status --short --branch
    sed -n '1,40p' LICENSE
    npx wrangler whoami
    npx wrangler versions list --name hyperopen --json
    dig NS dexhelm.com
    dig dexhelm.com
    dig testnet.dexhelm.com
    dig app.dexhelm.com
    dig status.dexhelm.com
    node .agents/skills/deploy-hyperopen-cloudflare/scripts/preflight.mjs
    npm run test:cloudflare-worker
    npm run test:release-assets
    npm run test:playwright:seo
    npm run build:cloudflare
    npm run white-label:validate -- --config config/white-label/dexhelm.json --origin https://testnet.dexhelm.com
    npm run verify:white-label -- --config config/white-label/dexhelm.json --origin https://testnet.dexhelm.com --output out/white-label/dexhelm
    PLAYWRIGHT_WHITE_LABEL_ROOT=out/white-label/dexhelm HYPEROPEN_EXPECT_BRAND=DEXHelm HYPEROPEN_EXPECT_TENANT_ID=dexhelm HYPEROPEN_EXPECT_ORIGIN=https://testnet.dexhelm.com HYPEROPEN_EXPECT_ENABLED_ROUTE=/trade HYPEROPEN_EXPECT_LOGO_URL=https://testnet.dexhelm.com/brand/dexhelm-mark.svg npm run test:playwright:white-label
    npm run build:cloudflare
    node .agents/skills/deploy-hyperopen-cloudflare/scripts/preflight.mjs --artifact
    npm run cloudflare:check
    env JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home PATH=/opt/homebrew/opt/openjdk@21/bin:$PATH NODE_OPTIONS=--localstorage-file=/tmp/hyperopen-node-gates-localstorage npm run gates

Start a freshly rebuilt local Worker on a free port and, from another shell, run:

    env JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home PATH=/opt/homebrew/opt/openjdk@21/bin:$PATH npm run cloudflare:dev -- --port 8790
    HYPEROPEN_VERIFY_ORIGIN=http://127.0.0.1:8790 npm run verify:deployment-headers
    HYPEROPEN_VERIFY_ORIGIN=http://127.0.0.1:8790 npm run verify:cloudflare-worker
    curl -sS -o /dev/null -w '%{http_code}\n' https://dexhelm.com/
    curl -sS -o /dev/null -w '%{http_code}\n' 'https://testnet.dexhelm.com/trade?hyperliquidNetwork=testnet'
    curl -sS -o /dev/null -w '%{http_code}\n' https://app.dexhelm.com/
    curl -sS -o /dev/null -w '%{http_code}\n' https://status.dexhelm.com/
    curl -sS https://testnet.dexhelm.com/api/health

Expected pre-Phase-9 public statuses are `200`, `200`, `503`, `200`, and health JSON `{"status":"ok"}`. The Mainnet-closed response must include `no-store`; Mainnet and generic proxy probes must be 404. Stop the local Worker cleanly before continuing.

For Milestone 5B, run the focused deterministic candidate tests first, then the release and browser evidence from `/Users/zh/Documents/Hyperopen`:

    npm run test:cloudflare-worker
    npm run test:release-assets
    npm run build:cloudflare:mainnet-candidate
    npm run test:playwright:mainnet-opening
    npm run cloudflare:check:mainnet-candidate
    env JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home PATH=/opt/homebrew/opt/openjdk@21/bin:$PATH NODE_OPTIONS=--localstorage-file=/tmp/hyperopen-node-gates-localstorage npm run gates

The Playwright command starts local HTTPS Workers with `wrangler.mainnet-opening.jsonc` and the appropriate `--local-upstream` host, so it must retain host-specific assertions rather than probing `127.0.0.1` directly. Record the document-discovered fingerprinted CSS/JavaScript paths and their exact cache values from both hosts. The dry-run must still read the candidate assets and exit without upload; it is not permission to deploy.

For Milestone 5C, run the focused funding tests and browser regression from `/Users/zh/Documents/Hyperopen` before the aggregate gates:

    npm run setup:worktree
    npm run test:runner:generate
    npx shadow-cljs --force-spawn compile test && node out/test.js
    npx playwright test tools/playwright/test/trade-regressions.spec.mjs --workers=1 --grep "funding modal.*withdraw|Bridge2 USDC withdrawal"
    npm run qa:design-ui -- --targets trade-route --manage-local-app
    npm run browser:cleanup
    npm run gates

The focused deterministic assertions must show the new positive sub-minimum USDC withdrawal path passing, the old `Minimum withdrawal is 5 USDC.` assertion removed, and the deposit `5 USDC` boundary unchanged. The focused Playwright command must exit `0` and show the Bridge2 summary rows at every exercised width without a `Minimum withdrawal` row or horizontal overflow. Browser QA must explicitly report visual, native-control, styling-consistency, interaction, layout-regression, and jank/perf as `PASS`, `FAIL`, or `BLOCKED`; any failure or block keeps the UI milestone incomplete. No publish, wallet confirmation, signer call, or Testnet withdrawal retry is part of this milestone.

For Milestone 5A, the `tdd_test_writer` must first add the approved RED tests and regenerate the runner. The RED run is successful only when the newly named Close All tests fail for the missing behavior, without changing production source; record any unrelated pre-existing failure separately. The `worker` then implements only the source surfaces named in Milestone 5A and repeats the complete deterministic suite. Run all commands from `/Users/zh/Documents/Hyperopen`:

    npm run setup:worktree
    npm run test:runner:generate
    npx shadow-cljs --force-spawn compile test && node out/test.js
    npm test

Add `test/hyperopen/account/history/close_all_positions_test.cljs` to prove pure snapshot collection includes default and named-perp-DEX rows only when their signed size is numeric and nonzero, preserves stable identity, makes the correct opposite-side absolute-size reduce-only IOC leg, and rejects a malformed candidate, market, asset, reduce-only, wire flag, or builder mismatch without returning a request. Extend `test/hyperopen/account/history/position_modal_actions_test.cljs` to prove trigger, Escape, dismiss, stale snapshot, submitting duplicate, read-only, spectate, and empty-state action results. Extend `test/hyperopen/views/account_info/tabs/positions/content_test.cljs` and `test/hyperopen/views/account_info/tabs/positions/desktop_actions_test.cljs` to prove the precise desktop availability gate, confirmation semantics, control labels, and read-only rendering. Add `test/hyperopen/core_bootstrap/order_effects/close_all_positions_test.cljs` to prove one request, all-accepted success plus refresh, partial acceptance error with counts, exchange/transport failure, and no mutation of `:order-form-runtime`. Extend `test/hyperopen/runtime/effect_adapters/order_test.cljs`, `test/hyperopen/runtime/effect_adapters/facade_contract_test.cljs`, `test/hyperopen/runtime/registry_composition_test.cljs`, `test/hyperopen/schema/contracts/action_args_test.cljs`, and `test/hyperopen/runtime/effect_order_contract_test.cljs` so every new adapter, registration, schema, and effect-order policy is proved. Regenerate `test/test_runner_generated.cljs` only with `npm run test:runner:generate`; do not edit it by hand.

After deterministic tests pass, add `tools/playwright/test/close-all-positions.spec.mjs` using the existing `HYPEROPEN_DEBUG` bridge and existing exchange simulators. It must create stable fixture positions without a real wallet or network request, run at `375`, `768`, `1280`, and `1440`, and assert exactly the Milestone 5A behaviors: no mobile affordance at 375/768; desktop confirmation before a simulated submit at 1280/1440; exactly one reduce-only batch after explicit confirmation; no request for a stale snapshot, read-only account, or spectate account; and visible pending/error/success state. Run:

    npx playwright test tools/playwright/test/close-all-positions.spec.mjs --workers=1
    npm run qa:design-ui -- --targets trade-route --manage-local-app
    npm run browser:cleanup
    npm run gates

The committed Playwright command must exit `0` with the four governed viewport cases passing. The browser-QA report must explicitly mark visual, native-control, styling-consistency, interaction, layout-regression, and jank/perf as `PASS`, `FAIL`, or `BLOCKED` at every governed width; any `FAIL` or `BLOCKED` keeps Mainnet blocked. `npm run browser:cleanup` must confirm that tool-created inspection sessions are stopped. `npm run gates` must finish with an all-PASS matrix, including `npm run check`, `npm test`, and `npm run test:websocket`.

Only after the Phase 8 owner signs and the maintainer explicitly authorizes Phase 9 in this session may the deploy boundary run:

    npx wrangler whoami
    npm run deploy:cloudflare

Capture Wrangler's exact Worker name, configured custom domains, and `Current Version ID`. Do not run these publish commands as part of the current documentation-only task.

## Validation and Acceptance

This plan remains active and incomplete while any checkbox above is unchecked. The immediate acceptance of this task is that this file exists at `docs/exec-plans/active/2026-08-05-dexhelm-mainnet-opening.md`, contains the supplied baseline/version and explicit approval boundaries, and leaves `app.dexhelm.com` closed. The plan itself is valid when `git diff --check -- docs/exec-plans/active/2026-08-05-dexhelm-mainnet-opening.md` and `npm run lint:docs` pass without changing any source or test file.

Phase 7 acceptance is the complete manual Testnet flow with independent API and chain evidence and a flattened account. Phase 8 acceptance is the signed production-readiness record plus current-version full gates. Phase 9 acceptance is a separately authorized release whose public host matrix proves Mainnet open and Testnet isolation; it never includes real-fund trading unless separately approved.

Milestone 5B acceptance is separately mandatory before Phase 8 can be considered ready. The generated root `out/cloudflare/dexhelm-mainnet-opening/_headers` must have an exact `! Cache-Control` plus `Cache-Control: public, max-age=31556952, immutable` block for every fingerprinted Mainnet CSS/JavaScript file beneath `/__hyperopen_mainnet/`, including the Mainnet document's actual `mainScriptHref`. The same public file requested as `https://app.dexhelm.com/js/main.<hash>.js` must receive that immutable value through the local Worker mapping. Testnet's public fingerprinted asset must remain immutable. A `200` response, a nested `_headers` file, a static text match without candidate verification, or a changed cache policy for metadata/service-worker paths does not meet acceptance.

Milestone 5A acceptance is separate and mandatory before Phase 9. At 1280 and 1440 pixels, a writable account with current positive and negative positions must show `Close All`; selecting it must show an accessible confirmation and must not send an order. The destructive confirmation must identify its position count, keep a visible Cancel control, and produce one batch only after the user activates `Close all positions`. Each emitted order must preserve the source DEX/market identity, reverse the signed source side, use the source absolute size, use immediate-or-cancel market behavior, and carry reduce-only `:r true`. The UI must say `Closing N positions...` while its one request is unresolved, `Close requests submitted for N positions` only after every response leg is accepted, and an actionable error with accepted/rejected counts for any partial, rejected, or transport failure. It must never claim the positions are flat until refreshed account data establishes that fact.

Milestone 5C acceptance is separate and observable in the funding modal. Selecting USDC for withdrawal must show `Estimated time` with value `3-5 minutes` and `Withdrawal fee` with value `1 USDC`; it must not show `Minimum withdrawal`. Entering `1 USDC` or any other finite positive amount below `5` must produce the existing `withdraw3` action without a client-side minimum error, while empty, malformed, zero, and over-balance inputs remain blocked. Selecting USDC for deposit must still show `Minimum deposit 5 USDC`, reject `1 USDC`, and accept `5 USDC`. The request object must continue to contain `{:type "withdraw3", :amount <string>, :destination <normalized-address>}` and downstream signing must continue to provide the official `time`/`nonce`, `signatureChainId`, and `hyperliquidChain` fields. HyperUnit asset minima, ETAs, and fees must remain unchanged. The focused Playwright funding regression and all six browser-QA passes at `375`, `768`, `1280`, and `1440` are required evidence; no real wallet or exchange operation is accepted as proof.

The same deterministic tests must demonstrate no outbound request when the account is read-only or spectating, when all positions are zero, when the confirmation snapshot has changed, when any candidate cannot be prepared, when builder metadata differs, or when a submit is repeated while pending. At 375 and 768 pixels the app must render neither a Close All trigger nor its confirmation and must preserve usable table/mobile layout without horizontal overflow. The exact new and extended test files listed in `Concrete Steps`, the four-viewport Playwright test, the complete six-pass browser-QA result, and the all-PASS gate matrix are required observable evidence; a unit-only result, a dialog screenshot, or a response toast alone is insufficient.

## Idempotence and Recovery

All inspection, DNS lookup, preflight, tests, builds, verifiers, and dry-runs are safe to repeat. `build:cloudflare` must start from a fresh or atomically replaceable tenant output because endpoint rewriting is intentionally single-use; if a build is interrupted, rerun the complete build and artifact verification rather than patching generated JavaScript by hand. Do not delete the Worker, detach routes, change DNS, or retry a wallet action after an ambiguous response.

Milestone 5B must regenerate the candidate through its staging-and-atomic-replace path; never hand-edit `out/cloudflare/dexhelm-mainnet-opening/_headers` or its manifest. If a header test or manifest verification fails, stop before dry-run, correct the shared generation rule, rebuild both release roots, and let the candidate builder recalculate the final artifact digests. The safe fallback is to retain the existing production Testnet-only `wrangler.jsonc` configuration and Mainnet `503`; no cache header change is allowed on a published Worker in this milestone.

Before a real release, identify the last verified prior version with `npx wrangler versions list --name hyperopen --json` and rehearse only the read-only rollback identification/verification. If an authorized release fails, stop, keep Mainnet closed, obtain explicit authorization for that exact prior version, run `npx wrangler rollback <prior-version-id> --name hyperopen --message "Rollback failed release <failed-version-id>"` without `--yes`, then rerun the exact origin and four-host matrix. Rollback does not reverse wallet signatures, orders, transfers, DNS changes, or third-party incidents.

The Close All implementation is repeatable only from a newly derived current snapshot. Dismissal before submission is a local state reset and may be repeated. After a stale-data, validation, or builder-compatibility error, the user must review current Positions and deliberately open a fresh confirmation; the implementation must not reuse a cached request. After a transport-ambiguous response or partial acceptance, it must remain in the visible error state, refresh account surfaces where safe, and must not retry automatically or infer which unconfirmed legs were accepted. Tests and the Playwright simulator must use only the existing debug bridge and exchange simulators; they must not invoke a wallet, signature prompt, or live exchange request.

The Bridge2 parity change is repeatable and data-only. If an official source updates its estimate or fee, update the single parity presentation helper and its focused tests, rerun the deterministic and browser checks, and record the new source date in this plan. If an exchange rejects a positive amount for an undocumented dynamic rule, preserve the upstream error and record the response as evidence; do not restore a guessed client-side `5 USDC` threshold. If the presentation rows or local preview regress, restore the prior generated candidate rather than retrying any wallet or bridge action.

## Artifacts and Notes

Record concise, secret-free evidence in this plan or a linked governed QA artifact:

- `git rev-parse HEAD`, branch/status, remotes, and first 40 lines of `LICENSE`.
- Wrangler account identity, Worker name, current version `f26e000b-532c-44cd-8de3-cd04aab40fb2`, selected rollback version, and version-to-host proof.
- Tenant config digest, `out/white-label/dexhelm` asset directory, manifest digest, and preflight summary.
- Deterministic test, Playwright, local Worker, dry-run, and all-PASS gate transcripts.
- The Mainnet cache-policy reproduction and repair evidence: document-discovered fingerprinted asset paths, root `_headers` blocks under `/__hyperopen_mainnet/`, exact Mainnet/Testnet `Cache-Control` values, candidate-manifest verification, and the local proxy/health `200/200/200/404` matrix.
- The Close All RED failure names, subsequent `npm run test:runner:generate` transcript, final deterministic test transcript, and an all-PASS `npm run gates` matrix.
- The committed `tools/playwright/test/close-all-positions.spec.mjs` output with one passing result at each of `375`, `768`, `1280`, and `1440`, plus the Browser QA report that records all six passes, each viewport, residual blind spots, and cleanup confirmation.
- Test-only request captures for a two-position batch showing preserved DEX/market identity, opposite side, absolute size, IOC market behavior, and `:r true`; separate captures proving stale, invalid, builder-mismatch, read-only, spectate, empty, and duplicate paths emitted no request, and partial-response and transport-error paths showed the required visible error.
- Bridge2 parity evidence: deterministic preview and view-model outputs showing a positive `1 USDC` withdrawal builds `withdraw3`, no USDC withdrawal minimum row/error exists, deposit `5 USDC` minimum remains intact, HyperUnit rows remain unchanged, and the summary renders `Estimated time / 3-5 minutes` plus `Withdrawal fee / 1 USDC`; focused Playwright output and the six-pass browser-QA report at all four governed widths.
- Public status/TLS/header/logo/health/proxy matrix for apex, Testnet, Mainnet app, and status hosts.
- Testnet API/chain evidence with wallet address, signatures, cookies, and response bodies redacted or omitted; include requested/delivered amounts and transaction hashes only as needed.
- External monitor URL, last probe, alert recipient, incident channel, rollback drill result, owner signoffs, compliance state, affiliate state, and current bundle measurement.

Known historical release evidence includes `docs/exec-plans/completed/2026-07-25-dexhelm-public-brand-release.md` (version `906ea1c8-e2cd-42d4-915f-db8d0f8ebe56`, Testnet-only host matrix, 34/34 gates) and `docs/runbooks/hyperliquid-testnet-implementation.md` (version `588a4869-597b-4fd0-b744-42a631b7ec6f`, historical Testnet flow notes). These records are useful rollback leads but are not substitutes for current-account Wrangler confirmation.

## Interfaces and Dependencies

The release boundary must continue to use `tools/cloudflare/build_dexhelm_release.mjs`, `tools/cloudflare/rewrite_hyperunit_release_endpoints.mjs`, `tools/cloudflare/verify_cloudflare_worker.mjs`, `tools/release-assets/verify_deployment_headers.mjs`, `workers/hyperopen-worker.mjs`, `wrangler.jsonc`, and the tenant validator/verifier. The Testnet transaction boundary must use the endpoint, signing, contract, and verification procedures in `docs/runbooks/hyperliquid-testnet-implementation.md`. The frozen test contract is in `tmp/multi-agent/dexhelm-mainnet-opening/approved-test-contract.json`; it is local orchestration evidence and must be reflected in committed tests or a governed artifact before handoff. Do not add a second mutable browser network selector, a Mainnet Worker binding, a wallet automation path, or a secret-bearing monitoring integration.

Milestone 5C depends on the funding boundary in `src/hyperopen/funding/domain/assets.cljs`, `src/hyperopen/funding/domain/preview.cljs`, `src/hyperopen/funding/application/modal_vm/amounts.cljs`, `src/hyperopen/funding/application/modal_vm/presentation.cljs`, `src/hyperopen/funding/application/modal_vm/models.cljs`, and the `src/hyperopen/views/funding_modal/withdraw.cljs` renderer. Preserve the `withdraw3` transport/signing contract implemented by `src/hyperopen/api/trading.cljs` and `src/hyperopen/utils/hl_signing.cljs`; only display facts and local amount validation may change. Test coverage belongs in the existing funding domain/application/view tests and `tools/playwright/test/trade-regressions.spec.mjs`; do not add live-network or wallet-extension dependencies.

For Close All, `src/hyperopen/account/history/close_all_positions.cljs` must expose a pure current-snapshot function and a pure batch-preparation function. The current-snapshot result is a vector of maps with exactly the stable identity needed to compare later: `:position-key`, `:coin`, `:dex`, and signed `:szi`. The preparation result is either `{:ok? true :request <single order-batch request> :snapshot <current snapshot>}` or `{:ok? false :display-message <human-readable reason>}`; it must never contain a valid subset request. `src/hyperopen/account/history/position_overlay_actions.cljs` must expose `trigger-close-all-positions [state & [trigger-bounds]]`, `dismiss-close-all-positions-confirmation [state]`, `handle-close-all-positions-confirmation-keydown [state key]`, and `submit-close-all-positions-confirmation [state]`. The state at `[:positions-ui :close-all-confirmation]` must carry `:open?`, `:lifecycle`, `:snapshot`, `:trigger-bounds`, `:error`, `:accepted-count`, and `:rejected-count`, where `:lifecycle` is only `:confirming`, `:submitting`, `:success`, or `:error` while open.

The one heavy effect id is `:effects/api-submit-close-all-positions`. Its adapter must be exported by both adapter namespaces and registered under the `:orders` effect group in `src/hyperopen/app/effects.cljs`; its effect-order policy must require the state projection before that one heavy I/O effect and disallow a duplicate. The effect input contains the immutable confirmed snapshot and the one validated batch request. It may call the existing authenticated signed-order API once, but must write lifecycle outcome only to the Close All model, retain response counts, and dispatch the existing account refresh behavior after accepted legs. The action schema and runtime registration must accept the existing zero-argument trigger and the optional numeric-bounds map without weakening unrelated action validation.

The plan has no additional performance implementation because a fresh same-workload rebuild now measures the DEXHelm branded and Mainnet candidate main modules at `635,653` gzip bytes, below the unchanged `640,000` target. Any future optimization must rerun that baseline and justify why a simpler code or asset reduction is insufficient.

Revision note: created 2026-08-05 from the direct maintainer request after inspecting the clean source baseline, checked-in DEXHelm Worker policy, artifact preflight, historical deployment records, and zero-to-live/Testnet runbooks. On the same date, the acceptance and edge-case proposals were merged into a frozen local contract and docs validation passed. Later Browser MCP inspection added two release-blocking UX TODOs: visible Mainnet network labeling and zero header-brand letter-spacing. On 2026-08-05, this active plan was refreshed after reproducing the desktop Positions Close All no-op; it defined the worker-owned confirmation, reduce-only batch, lifecycle, deterministic coverage, Playwright, and browser-QA contract. The frozen RED suite was materialized, then the worker implemented the approved source-only repair. On 2026-08-06, the fixture and theme-policy defects were repaired, Trusted Types worker paths were narrowed and covered, focused Playwright passed `4/4`, governed browser QA passed, the matrix passed `35/35`, the public monitor matched `200/200/503/200/200`, and the rebuilt candidate dry-run exited `0`. Later on 2026-08-06, a local-Wrangler Mainnet probe identified the missing root `__hyperopen_mainnet` immutable cache rules. Milestone 5B confined the repair to shared release/header generation, added deterministic and browser regression evidence, and restored exact immutable caching on Mainnet/Testnet fingerprinted assets; Worker `49/49`, Playwright `8/8`, dry-run, and the complete `35/35` matrix pass. A fresh same-workload release measurement then placed the branded/Mainnet candidate at `635,653` gzip bytes, below the unchanged budget. On 2026-08-06, Milestone 5C was added to align the Bridge2 USDC withdrawal display and validation with the current official source set: `1 USDC` withdrawal fee, a combined `3-5 minutes` estimate, no undocumented USDC withdrawal minimum, unchanged `5 USDC` deposit minimum, unchanged HyperUnit asset rules, and unchanged `withdraw3` signing schema. Remaining blockers are legal approval, named monitoring/incident ownership and independent outage communication, Testnet withdrawal settlement, this parity implementation/QA milestone, and any other unchecked Phase 8 owner item. No wallet, exchange, Cloudflare upload, DNS, or fund state was changed.
